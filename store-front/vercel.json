{
  "version": 2,
  "builds": [
    {
      "src": "package.json",
      "use": "@vercel/static-build",
      "config": {
        "distDir": ".next"
      }
    }
  ],
  "routes": [
    {
      "src": "^(?!/(api|_next|static|favicon.ico|robots.txt)).*$",
      "dest": "/index.html"
    }
  ]
}